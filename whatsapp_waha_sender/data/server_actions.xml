<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="server_action_mass_whatsapp_crm" model="ir.actions.server">
        <field name="name">Send WhatsApp (Mass)</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="binding_model_id" ref="crm.model_crm_lead"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code"><![CDATA[
action = {
    'type': 'ir.actions.act_window',
    'name': 'Mass WhatsApp',
    'res_model': 'waha.mass_composer',
    'view_mode': 'form',
    'target': 'new',
    'context': dict(env.context, default_res_model='crm.lead',
                    default_res_ids=','.join(str(i) for i in env.context.get('active_ids', []))),
}
]]></field>
    </record>

    <record id="server_action_mass_whatsapp_sale" model="ir.actions.server">
        <field name="name">Send WhatsApp (Mass)</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code"><![CDATA[
action = {
    'type': 'ir.actions.act_window',
    'name': 'Mass WhatsApp',
    'res_model': 'waha.mass_composer',
    'view_mode': 'form',
    'target': 'new',
    'context': dict(env.context, default_res_model='sale.order',
                    default_res_ids=','.join(str(i) for i in env.context.get('active_ids', []))),
}
]]></field>
    </record>

    <record id="server_action_mass_whatsapp_account" model="ir.actions.server">
        <field name="name">Send WhatsApp (Mass)</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code"><![CDATA[
action = {
    'type': 'ir.actions.act_window',
    'name': 'Mass WhatsApp',
    'res_model': 'waha.mass_composer',
    'view_mode': 'form',
    'target': 'new',
    'context': dict(env.context, default_res_model='account.move',
                    default_res_ids=','.join(str(i) for i in env.context.get('active_ids', []))),
}
]]></field>
    </record>

    <record id="server_action_mass_whatsapp_partner" model="ir.actions.server">
        <field name="name">Send WhatsApp (Mass)</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code"><![CDATA[
action = {
    'type': 'ir.actions.act_window',
    'name': 'Mass WhatsApp',
    'res_model': 'waha.mass_composer',
    'view_mode': 'form',
    'target': 'new',
    'context': dict(env.context, default_res_model='res.partner',
                    default_res_ids=','.join(str(i) for i in env.context.get('active_ids', []))),
}
]]></field>
    </record>
</odoo>
